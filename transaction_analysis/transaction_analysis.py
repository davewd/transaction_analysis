# -*- coding: utf-8 -*-

"""Main module."""

def normalize_input_data(dataFrame) :
    """[summary]
    
    Arguments:
        dataFrame {pandas.DataFrame} -- data to be normalized in dataframe format.
    """